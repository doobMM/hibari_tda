# 논문-JSON 데이터 정합성 전수 검증 결과 (2026-04-17)

## 검증 범위
두 논문(full.md, short.md)의 모든 수치 표 vs docs/step3_data/ JSON 14개 파일

## 결과 요약
- **일치 14개**: §4.1~§4.2, §4.5a, §6.1~§6.3, §6.7.1~§6.9 등
- **불일치 5개**: 전부 JSON이 stale(낙후)한 경우 → 논문 수정 불필요

## 불일치 상세

### ① ② ③ ⑤: JSON stale, 논문 정확
- ① §4.4: JSON이 버그픽스(d09e756) 재실행으로 덮어쓰임. git 478d743 원본 = 논문 일치.
- ② §7.2 / ③ §7.3: JSON이 n=2 초기 테스트. 논문은 N=10 정식 실험.
- ⑤ §7.7: JSON이 버그픽스 이후값. 출력 .mid 파일(m00_s9302_20260408)로 논문값 독립 검증됨.

### ④ §7.5 improvements: 유일한 "판단 필요" 항목
- 논문: P3+C=0.0590★ (버그픽스 이전 설정)
- JSON(step71_improvements.json): P4+C=0.0654 (버그픽스 이후)
- **판정: Option B 채택** — feedback_exclude_bug_fix.md에 따라 버그픽스는 논문 제외 확정.
  논문 전체가 버그픽스 이전 파이프라인 기준으로 일관됨(§4.1도 동일). 수정 불필요.

## 결론
논문 수치 전체가 내부적으로 일관됨. JSON 일부가 stale 상태이지만 논문에는 영향 없음.
