4.
- tonnetz_noe_distance(n1, n2)의 출력이 실수라는데, duration은 어떻게 고려돼?
- voice_leading_note_distance(n1, n2)의 출력은 정수여야 하지 않아? 반음 차이만 고려하는 데. 그리고 여기서도 duration은 어떻게 고려돼?
- dft_note_distance(n1, n2)는 어떻게 계산되서 실수가 나와? 예시를 들어줄래?
- 두 note간 확장에서 옥타브와 duration 정보 고려할 때 우변에서 2, 3번째 summand 왜 그렇게 설정했는지 설명이 필요해. 게다가 계수는 임의로 설정해놓고 grid-search를 수행하지도 않았잖아.


4.1
- frequency로 거리를 삼았을 때 '발견 cycle 수'가 43이라고 했는데, 중첩행렬 구하는 함수 수정하고 나서 1로 바뀌었다고 하지 않았어?

4.2
- K=10과 K=17에 대해 처음 K개라고 했는데 '처음'이 무엇에 기반하는지 궁금해!

4.3
- welch-satterthwaite 자유도 구할 때 v를 37.9라고 계산했는데 계산 과정 다시 한 번 보여줄래?

4.3a
- 중첩행렬 구축하는 로직에서 오류 수정한 이후에도 여전히 tonnetz가 가장 강한 baseline으로 나왔어?
- tonnetz를 가장 강한 baseilne으로 도출한 게 3.1절이 아니라 4.1절로 바뀐 거지?
- 다섯 가지 설정에 대해 algorithm 1만 N=20회 반복 실행했는데, algorithm 2에 대해선 실행 안 했어?
- "cycle vertex의 절반 이상이 활성일 때만 그 cycle을 살아 있는 것으로 간주"한다고 0.5 임계값을 적용한 실험에 대해 해석했는데, 일단 cycle note의 희귀도가 다 다른 것임을 고려할 때 틀린 서술이라고 생각해, 빼든가 수정해줘.


3.4a
- 4.4 다음에 나오는데 넘버링 순서에 유의해줘.
- 7.15에서 제안된 개선 F를 3.4(4.4a)에서 언급하는 건 순서가 이상한 것 같아. 재정렬해줘.
- continuous activation matrix는 continuous direct 케이스를 말하는 게 맞아?
- 왜 개선 F라고 하는지 네이밍의 근거를 모르겠어.
- '기존 모든 결과와의 통합 비교'에서 7.1.6 Algo 1과 7.1.8 Algo 1 케이스가 있는데 해당 내용을 앞으로 가져오던가 아니면 해당 결과를 뒤로 빼든가 해야 할 것 같아. 난 개인적으로 전자를 선호해!

3.6.1
- 4.5 다음에 나오는데 넘버링 순서에 유의해줘.
- '이 집합의 **interval vector** — 각 음정 클래스(1~6 반음)가 집합 안에서 몇 번 등장하는지를 센 벡터 — 는 $[2, 5, 4, 3, 6, 1]$이다.'라고 하는데, 여기서 "집합"이 한 모듈을 말하는 건지 확인 필요하고, 빈도를 측정하는 거라면 7개 원소가 있어야 할 텐데 6개만 있는 이유도 알아야 해.

3.6.2
- 넘버링 순서에 유의해줘.
- Shannon entropy가 뭔지 설명을 덧붙이거나, 아니면 그냥 pitch entropy의 근거에 대해서만 설명해주면 돼.
- unique pitch count가 띄어쓰기가 안 된 것으로 보여. 수정해줘.

3.6.3
- voice-leading 최적 (3.1 vs 7.2)에서 7.2 내용을 앞으로 땡겨오는 게 필요할 것 같아.

3.6.6 세곡의 비교 요약
- 흐름을 끊는 것 같아서 뺐어.


5. 시각자료
- 개인적으로 맨 앞으로 가져가는 건 어떨지 조심스레 제안해봐.
- Figure 2가 tonnetz에 기반해서 만들어졌는지 궁금해. 왜냐하면 중첩행렬 구축 로직이 바뀌어서 만약에 frequency 기반이라면 예전만큼 cycle이 추출되지 않을 것이기 때문이거든.
- Figure 3에서 '전체 곡에서 해당 곡 내 7~11회 출현하는 건지' 아니면 inst 1/2의 특정 구간에서 그런 건지 알려줄래?
- Figure 4에서 '본 연구의 주 거리 함수는 __Tonnetz hybrid distance__ ($\alpha = 0.5$)'라고 하는데 사실 alpha가 앞에선 0.3이라고 했었거든. 그래서 근거에 기반해서 통일이 필요할 것 같아. 만약 0.3이 맞다면 해당 figure에 업데이트 필요할 것 같고.
- Figure 7는 해당 figure 모아놓은 섹션 중에서도 앞으로 땡겨서 우선적으로 이 곡의 구조를 알아보게 하자. 그리고 해당 figure에서 inst 1이 module 5가 나오고 있을 땐 inst 2에선 module 4가 나오고 있어야 맞지 않을까? 또한 (a) inst 1 설명에서 '옅은 초록 배경은 모듈 영역이다'라고 했는데 inst 2에서도 빨간색 경계마다 해당 초록색 선이 맞춰줘 있어야 할 것 같아.

6.1
- $$
\text{\{수만 곡의 MIDI 코퍼스\}} \;\to\; \text{신경망 학습} \;\to\; \text{샘플링 생성}
$$
라고 마크다운에선 텍스트들이 표시되는데 실제 보고서 pdf에선 해당 텍스트들이 보이지 않아.




[ 추가 과제 ]
- 3.4a(→4.4a) 더 정교한 가중치 학습이나 soft activation을 받아들이는 algorithm 2 변형을 통해 추가 향상이 가능할 것으로 기대된다.
- Debussy "Clair de lune" / Ravel "Pavane pour une infante defunte")