academic_paper_full.md에서 바뀐 목차 넘버링을 기준으로 다시 md 파일을 각 목차별로 만들고 pdf로도 바꿔줘.

1.2 
- 이 곡을 선택한 이유에 '단선율의 국악에 TDA를 적용한 정재훈 교수의 선행연구를 화성음악으로 확장함에 있어 복잡성을 내포하면서도 일정한 패턴이 있어 모델링이 용이했기 때문'이라는 내용 추가해줘.
- 두 악기가 서로 다른 역할을 맡아 → 두 악기가 곡 내에서 한 번도 같은 위상으로 겹치지 않게 배치되어
- 충분히 풍부한 위상 구조를 드러낸다. → 복잡성을 갖는다.
- 작곡가 본인의 작곡 방식 기록 제외

1.3
- 1.2 앞으로 옮기기
- '거리 함수 baseline' 이란 표현이 어색하니 변경

2. 수학적 배경
- 이 링크(https://github.com/postech-minds/postech-minds/blob/main/tutorials/%5BGTDA_TUTO%5D01-Introduction_to_TDA.ipynb)를 적당한 위치에 추가해서 사람들이 접근할 수 있게 해줘.
- 2.1의 filtration 설명에서 등장하는 VR_(epsilon_0)(X)가 사실상 X야? epsilon_0 = 0일 때 모든 0-simplex는 포함되지않아

2.2
- n차원 구멍: 최소 n+2개의 점이 필요하다 → 이거 사실이 맞는지 체크해줘.
- 1차원 cycle을 점 3개 케이스로 설명하고 있는데, Euclidean 공간이 아님과 path-dependent distance에 대한 설명이 추가적으로 필요할 것 같아, 첨부한 pdf 봐줘.
- H_n(K)에 rank를 적용할 수 있다는 것은 H_n(K)가 함수라는 거야?
- 선형대수학에 기반해 boundary 함수로부터 H_n(K) 구하는 예시 고안해줄래?


2.4
 - Tonnetz 거리에 대해서 최솟값과 최댓값이 얼마인지도 정의가 나온 다음 '빈도 기반 거리' 나오기 전에 적어줘.
- DFT distance 설명할 때 Fourier space로 옮기는 이유를 말해줘. 그리고 Fourier space가 무엇인지도 말해주고. 푸리에 계수에 대한 설명도 무슨 뜻인지 잘 이해하지 못하겠어.
- Pitch class 집합을 왜 함수로 볼 수 있는지 잘 모르겠어. L_2 거리가 무엇인지도 잘 모르겠어.
- hibari에 대해 alpha = 0.3이 최적임을 확인했다는 데 이거 다시 확인한 거 맞아?

2.5
- 목표 ON 비율의 근거 앞뒤로 **가 표시되고 있는데 수정해줘.

2.6
- KL Divergence가 왜 항상 0보다 크거나 같은지 예시를 추가해주면 좋을 것 같아.
- D_JS를 metric으로 사용하지 않고 그냥 원곡과 생성된 음악 간의 유사도 평가지표로 사용한다면 metric 관련 내용은 제외해도 될 것 같아.
- Transition 빈도 분포는 순서를 고려하는 거 맞아?
- '본 연구의 최우수 조합(Tonnetz hybrid + FC, $\alpha = 0.3$)에서 pitch JS divergence는 $D_{\text{JS}} \approx 0.002$를 달성하였다. 이는 가능한 최댓값 $\log 2 \approx 0.693$의 약 $0.3\%$에 해당하는 값이다.'라고 했는데, 이거 중첩행렬 구축 과정에서 오류 발견한 후에도 유지된 내용 맞는지 확인해줘.

2.7
- monotone이라는 표현 말고 '단조'라는 말 포함하게 바꿔줘.
- S = subset of C, the set of cycles ?
- Note pool Jaccard similarity에서 N_full과 N_s가 처으 정의될 때 식이 너무 작게 나와.
- J(S)가 실제로 수식과 같이 정의돼?
- Pearson 상관계수가 어떻게 정의돼? 예시도 같이 적어줄 수 있어?
- betti curve similarity에서 단순히 cycle의 수가 아니라 어떤 cycle들이 살아있는지 비교해야 하지 않아? 생성된 곡도 inst 1/ 2가 나뉘어? rate를 적용할 수 있게? 어떤 cycle들이 살아있는지 비교하지 못한다면 그냥 전체 골격을 비교해야 옳지 않을까?

2.8
- L_BEC가 실제로 어떤 식으로 작동하는 건지 예시와 함께 적어주면 좋겠어. 
- 학습 단계에서 BCE 손실로 모델 파라미터를 업데이트한다고 했는데, 어떻게 해? gradient 기반으로?

2.9
- "두 악기를 동시에 적기보다는, 먼저 한 악기의 선율선을 시간 방향으로 충분히 채워 넣은 뒤, 다른 악기를 그 위에 겹쳐 배치하는 방식"라고 했다는데 rephrase한 거야? 그에 상응하는 인터뷰를 찾을 수가 없어? 정확한 출처와 함께 명기해줘.
- inter weight를 구할 때 정말 lag 1~4를 같은 가중치로 더해? lag가 작을 수록 가중치가 크게 하고 4개 가중치의 합이 1이 되도록 파이프라인을 수정해야 하지 않을까?
- r_c의 의미는 설명하는데 r_t의 의미는 설명이 안 돼 있어. r_t가 작을 때, 1일 때, 1보다 클 때의 의미를 각각 설명해줘.
- d(n_i, n_j) 정의할 때 [4pt]가 갑자기 튀어나와서 출력한 보고서에서 보이는데 지워야하지?
- d_inf를 정의할 때 step이 나오는데 이게 뭔지 모르겠어.
- 발음/발성을 모두 '타건'으로 바꿨어.

3.1
- 규칙 1에서 교집합이 공집합이면 전체 note pool P에서 균등 추출하는 게 아니라, 해당 cycle들의 합집합을 만든 다음에 거기서 뽑아야 맞지 않을까?

3.2
- "연구의 실험에서는 이러한 일반화가 실제로 관측되었으나, 이는 "위상 구조 자체의 수학적 보존"이라기보다 "학습된 매핑의 통계적 일반화"에 가깝다."라는 말 모호해서 수정이 필요할 것 같아. 아니면 그냥 지우던가. "학습된 매핑의 통계적 일반화"가 모호하게 들려.
- 입력 형태에서 B가 들어가는 이유를 모르겠어.
- 파라미터 수 산정 근거가 뭐야?
- hidden state와 "주목도 점수"는 각각 어떻게 정의돼?
- 'PyTorch의 `BCEWithLogitsLoss`는 sigmoid를 손실 안에 포함시킨 수치 안정 버전이며, 양성/음성 클래스의 불균형을 보정하기 위해 `pos_weight` 파라미터에 (음성 샘플 수 / 양성 샘플 수) 비율을 넣는다.'에서 수치 안정이라든가, 양성/음성 클래스라든가 무슨 말인지 모르겠거든? 각각이 무엇인지 자세히 설명을 추가하든가, 아니면 그냥 기능적으로 해당 함수가 무엇을 수행하는지 알려주는 것으로 충분해.
- 5단계 onset gap 후처리는 Algorithm 1/2 둘 다에 적용돼?





[ 후속연구 사항 ]
- J, C, B grid search